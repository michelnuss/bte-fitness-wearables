# Copy this file to `.env` and fill in your values (never commit `.env`).
# See GOOGLE_CLOUD_SETUP.md for how to create an API key.

GOOGLE_API_KEY=your_key_here

# Optional — default is gemini-2.5-flash (gemini-2.0-flash is not available to new API users)
# GEMINI_MODEL=gemini-2.5-flash
